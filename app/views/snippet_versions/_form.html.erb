<%= form_for(snippet_version) do |f| %>
  <% if snippet_version.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(snippet_version.errors.count, "error") %> prohibited this snippet_version from being saved:</h2>

      <ul>
      <% snippet_version.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content %>
    <%= f.text_area :content, class:"codemirror-content", data: {cdMirrorMode: "javascript"} %>
  </div>

  <div class="field">
    <%= f.label :version %>
    <%= f.text_field :version %>
  </div>

  <div class="field">
    <%= f.label :comment %>
    <%= f.text_field :comment %>
  </div>

  <%= f.hidden_field :snippet_id %>
  <div class="field">
    <%= f.label :doc %>
    <%= f.text_field :doc %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
